# Payload Pattern


```
import { getPayload } from 'payload'
// this is how you import config
import config from '@payload-config'

const seed = async () => {
  // Get a local copy of Payload by passing your config
  const payload = await getPayload({ config })
...
```
